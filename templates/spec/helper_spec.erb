require 'rails_helper'
#   - generated from builder/templates/spec/helper_spec.erb
# Specs in this file have access to a helper object that includes
# the <%= model_name %>sHelper. For example:
#
# describe <%= model_name %>sHelper do
#   describe "string concat" do
#     it "concats two strings with spaces" do
#       expect(helper.concat_strings("this","that")).to eq("this that")
#     end
#   end
# end
RSpec.describe <%= namespace.nil? ? '' : namespace.capitalize + '::' %><%= model_name %>sHelper, :type => :helper do
  pending "add some examples to (or delete) #{__FILE__}"
end